{
  "code": "async () => { const source = `function headers(request, env) { const origin = request.headers.get('Origin'); if (origin !== env.ALLOWED_ORIGIN) return { Vary: 'Origin' }; return { 'Access-Control-Allow-Origin': origin, 'Access-Control-Allow-Methods': 'POST, OPTIONS', 'Access-Control-Allow-Headers': 'Content-Type', 'Access-Control-Max-Age': '86400', Vary: 'Origin' }; } function reply(body, status, request, env) { return new Response(JSON.stringify(body), { status, headers: { 'Content-Type': 'application/json; charset=UTF-8', ...headers(request, env) } }); } export default { async fetch(request, env) { const url = new URL(request.url); const origin = request.headers.get('Origin'); if (request.method === 'OPTIONS') { if (origin !== env.ALLOWED_ORIGIN) return new Response(null, { status: 403, headers: { Vary: 'Origin' } }); return new Response(null, { status: 204, headers: headers(request, env) }); } if (url.pathname === '/api/health' && request.method === 'GET') return reply({ status: 'ok', service: 'zayaans-signature-api' }, 200, request, env); if (url.pathname !== '/api/newsletter') return reply({ error: 'Not found' }, 404, request, env); if (request.method !== 'POST') return reply({ error: 'Method not allowed' }, 405, request, env); if (origin !== env.ALLOWED_ORIGIN) return reply({ error: 'Origin not allowed' }, 403, request, env); let body; try { body = await request.json(); } catch { return reply({ error: 'Invalid JSON request body' }, 400, request, env); } const email = typeof body.email === 'string' ? body.email.trim().toLowerCase() : ''; if (!/^[^\\s@]+@[^\\s@]+\\.[^\\s@]{2,}$/i.test(email) || email.length > 254) return reply({ error: 'A valid email address is required' }, 422, request, env); await env.NEWSLETTER.prepare(\"INSERT INTO newsletter_subscribers (email, source, consent_at) VALUES (?, 'website', CURRENT_TIMESTAMP) ON CONFLICT(email) DO UPDATE SET source = excluded.source, consent_at = excluded.consent_at\").bind(email).run(); return reply({ success: true }, 201, request, env); } };`; const metadata = { main_module: 'worker.js', compatibility_date: '2025-09-01', bindings: [{ type: 'd1', name: 'NEWSLETTER', id: 'ad1c0c18-5711-4ede-b15e-be289963875d' }, { type: 'plain_text', name: 'ALLOWED_ORIGIN', text: 'https://bayzed123.github.io' }] }; const boundary = `---zayaan${Date.now()}`; const body = [`--${boundary}`, 'Content-Disposition: form-data; name=\"metadata\"', 'Content-Type: application/json', '', JSON.stringify(metadata), `--${boundary}`, 'Content-Disposition: form-data; name=\"worker.js\"; filename=\"worker.js\"', 'Content-Type: application/javascript+module', '', source, `--${boundary}--`, ''].join('\\r\\n'); return cloudflare.request({ method: 'PUT', path: `/accounts/${accountId}/workers/scripts/zayaans-signature-api`, body, contentType: `multipart/form-data; boundary=${boundary}`, rawBody: true }); }"
}

