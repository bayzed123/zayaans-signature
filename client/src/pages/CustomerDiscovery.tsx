import FashionHeader from "@/components/FashionHeader";
import { SiteLink as Link } from "@/components/SiteLink";
import { CUSTOMER_DISCOVERY_LINKS, DISCOVERY_PAGES, isDiscoveryKey } from "@/lib/customerDiscovery";
import { ArrowRight, Facebook, Mail, MessageCircle } from "lucide-react";
import { useRoute } from "wouter";

const facebook = "https://www.facebook.com/share/1DbpHnT9DS/?mibextid=wwXIfr";
const email = "mailto:hello@zayaanssignature.com";

export default function CustomerDiscovery() {
  const [, params] = useRoute("/discover/:focus");
  const focus = isDiscoveryKey(params?.focus) ? params.focus : "offers";
  const page = DISCOVERY_PAGES[focus];
  const contact = focus === "contact";

  return <div className="min-h-screen bg-[#f5f2ec] text-[#171512]"><FashionHeader /><main className="pt-[76px]"><section className="border-b border-white/10 bg-[#151310] px-5 py-16 text-white sm:px-8 lg:px-12 lg:py-24"><div className="mx-auto max-w-[1440px]"><p className="section-kicker text-[--gold]">{page.eyebrow}</p><h1 className="mt-5 max-w-4xl font-display text-[clamp(3.8rem,8vw,7.4rem)] leading-[.82] tracking-[-.06em]">{page.title}</h1><p className="mt-8 max-w-2xl font-ui text-sm leading-7 text-white/70">{page.description}</p></div></section><section className="mx-auto max-w-[1440px] px-5 py-10 sm:px-8 lg:px-12 lg:py-14"><nav aria-label="Customer discovery" className="flex flex-wrap gap-2 border-b border-black/10 pb-8">{CUSTOMER_DISCOVERY_LINKS.map((link) => <Link key={link.key} href={link.href} className={`min-h-10 border px-3 py-2 font-ui text-[9px] font-bold uppercase tracking-[.14em] transition-colors ${focus === link.key ? "border-[#171512] bg-[#171512] text-white" : "border-black/15 text-black/58 hover:border-[#8f6b2c] hover:text-[#8f6b2c]"}`}>{link.label}</Link>)}</nav><div className="grid gap-10 py-14 lg:grid-cols-[1.1fr_.9fr] lg:items-end"><div><p className="section-kicker">Customer discovery</p><h2 className="mt-5 font-display text-[clamp(3rem,6vw,5.4rem)] leading-[.86] tracking-[-.05em]">A clear route to the <em className="font-normal text-[#8f6b2c]">right next step.</em></h2><p className="mt-7 max-w-xl font-ui text-sm leading-7 text-black/62">Every destination is intentionally direct. The storefront shows live products and proven services today, and names planned features plainly rather than pretending customer data or promotions already exist.</p>{page.actionHref.startsWith("http") ? <a href={page.actionHref} target="_blank" rel="noreferrer" className="gold-button mt-9">{page.actionLabel} <MessageCircle size={15} /></a> : <Link href={page.actionHref} className="gold-button mt-9">{page.actionLabel} <ArrowRight size={15} /></Link>}</div><aside className="border border-black/12 bg-[#e8e0d3] p-7 sm:p-9"><p className="section-kicker">Useful paths</p><div className="mt-6 space-y-4 font-ui text-sm leading-6 text-black/68"><Link href="/" className="flex items-center justify-between border-b border-black/10 pb-4 hover:text-[#8f6b2c]">Home <ArrowRight size={15} /></Link><Link href="/collection" className="flex items-center justify-between border-b border-black/10 pb-4 hover:text-[#8f6b2c]">Shop all pieces <ArrowRight size={15} /></Link><Link href="/track" className="flex items-center justify-between border-b border-black/10 pb-4 hover:text-[#8f6b2c]">Track an order <ArrowRight size={15} /></Link><Link href="/cart" className="flex items-center justify-between hover:text-[#8f6b2c]">Your bag <ArrowRight size={15} /></Link></div>{contact && <div className="mt-8 grid gap-3 border-t border-black/10 pt-7"><a href={page.actionHref} target="_blank" rel="noreferrer" className="inline-flex items-center gap-3 font-ui text-[10px] font-bold uppercase tracking-[.16em] text-[#171512] hover:text-[#8f6b2c]"><MessageCircle size={15} /> WhatsApp</a><a href={facebook} target="_blank" rel="noreferrer" className="inline-flex items-center gap-3 font-ui text-[10px] font-bold uppercase tracking-[.16em] text-[#171512] hover:text-[#8f6b2c]"><Facebook size={15} /> Facebook</a><a href={email} className="inline-flex items-center gap-3 font-ui text-[10px] font-bold uppercase tracking-[.16em] text-[#171512] hover:text-[#8f6b2c]"><Mail size={15} /> Email</a></div>}</aside></div></section></main></div>;
}
